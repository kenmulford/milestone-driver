# OS cruft
.DS_Store
Thumbs.db
desktop.ini

# Editor / IDE
.vscode/
.idea/
*.swp
*~

# Logs and scratch
*.log
/tmp/
/scratch/

# milestone-driver tests-green stamp
/.milestone-driver-tests-stamp

# milestone-driver first-run preflight notice marker (per-clone, one-time)
/.milestone-driver-preflight-notice

# milestone-driver Trello integration one-time notice marker (per-clone)
/.milestone-driver-trello-notice

# milestone-driver triage result cache (per-clone)
/.milestone-driver-triage-cache.json

# milestone-driver parallel-mode worktree fleet scratch dir (per-clone)
.milestone-driver-worktrees/
