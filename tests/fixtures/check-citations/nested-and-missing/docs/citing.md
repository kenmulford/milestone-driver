# Citing

An anchor holding its own parens: `src/target.sh (extract_symbols() {)`.
An anchor holding a nested parenthetical: `src/target.sh (Fail-loud (fail-CLOSED))`.
An anchor whose target file does not exist: `src/gone.md (any anchor at all)`.
