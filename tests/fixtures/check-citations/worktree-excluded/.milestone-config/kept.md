# Kept

Grounded in `src/target.md (unique-anchor-alpha)`.

Only the worktrees/ subtree is excluded, never `.milestone-config/` as a whole -
the committed driver profile lives here and is a real citation source.
