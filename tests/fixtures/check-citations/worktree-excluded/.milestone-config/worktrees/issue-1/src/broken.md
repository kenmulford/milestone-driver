# A worktree's own copy of the repo

This whole subtree is a SECOND CHECKOUT the driver built for one issue. Scanning
it double-counts every citation in it, so it is excluded as a source.

Grounded in `src/target.md (anchor-that-does-not-exist)`.
