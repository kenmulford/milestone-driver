# A sibling plugin's per-run plan file

milestone-feeder writes its plan here, gitignored, once per run. A plan
describes the tree as it WAS at planning time, so its citations go stale by
design as the tree moves under it.

Grounded in `src/target.md (anchor-that-does-not-exist)`.
