# A resolved issue body, authoring scratch

Written during one feeder run and never refreshed afterwards. Anchors it names
are pinned to the tree at authoring time, which is exactly why this subtree is
a walk exclusion and not a citation source.

Grounded in `src/target.md (anchor-that-does-not-exist)`.
